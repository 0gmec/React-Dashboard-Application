import { useState } from "react"
import type { TaskFilterData } from "../../types"



export default function TaskFilterData() {
    const [status, setStatus] = useState()
    const [priority, setPriority] = useState()
    
//     const filters = {
//         "status": ["Pending", "In-Progress", "Incompleted", "Overdue"],
//         "priority": ["low", "medium", "high"]
//     }

//     const filteredTasks = all.filter((task) => {
// const filterStatus= status === "All" || task.status === status
// const filterPriority= priority === "All" || task.priority === priority
// return filterStatus && filterPriority
//     }
   
    
        
}